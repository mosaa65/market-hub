# خطة الصيانة الشاملة لنظام فورتيكس ERP (Vortex ERP)

> **تاريخ التحديث:** سبتمبر 2026  
> **الفرع المنفذ عليه:** `feature/perf-and-auth-optimization`  
> **تنبيه أمان صارم:** لا يتم إجراء أي حذف أو تعديل أو تفريغ لجداول أو سجلات الشركة الحقيقية (Strictly Zero Data Loss). التعديلات هندسية على مستوى كود الواجهة وإدارة الذاكرة والاتصال اللحظي.

---

## ملخص المراحل وحالة التنفيذ

| المرحلة | الوصف | الحالة |
|---|---|---|
| **المرحلة 1** | حل بطء التحقق من الصلاحيات وتجديد الجلسة (SWR Cache) | ✅ منجزة |
| **المرحلة 2** | توحيد شريط التمرير وإصلاح تجاوب الشاشات ونقاط البيع | ✅ منجزة |
| **المرحلة 3** | موزع التحديث اللحظي الذكي (Realtime) وتحديث المكونات المستهدفة فقط | ✅ منجزة |
| **المرحلة 4** | ضبط TanStack Query والكاش الذكي وتقليل استدعاءات الخادم | ⏳ قادمة |
| **المرحلة 5** | تقسيم الحزم (Code Splitting) وتحسين خطوط النظام وزمن التحميل الأولي | ⏳ قادمة |

---

## تفاصيل المرحلة الأولى (تمت): تسريع الصلاحيات ومنع الوميض
1. **استراتيجية Stale-While-Revalidate (SWR):** حفظ صلاحيات المستخدم (`roles` و`platform_admins`) محلياً.
2. **منع إعادة تركيب الواجهة (No Unmounting):** عند تجديد الـ token (`TOKEN_REFRESHED`) تظل الواجهة مستقرة دون ظهور spinner المتكرر المزعج.
3. **تزامن التحقق بالخلفية:** تشغيل استعلامات الصلاحيات الثلاث بالتوازي دون تعطيل المستخدم.

---

## تفاصيل المرحلة الثانية (تمت): حل مشكلة تعدد أشرطة التمرير وتجاوب الهيدر والفوتر
1. **توحيد سياق التمرير:** جعل حاوية التمرير واحدة فقط (`main`) وإلغاء التمريرات المتضاربة.
2. **نظام مخصص لنقاط البيع (POS):** عزل واجهة الكاشير بـ `overflow-hidden` مرن يمنع ظهور شريط التمرير المزدوج ويخفي الفوتر الخارجي لتوفير أقصى مساحة ممكنة على الجوالات والشاشات الصغيرة.
3. **شريط تمرير نحيف ومتناسق (`custom-scrollbar`):** متوافق مع الوضع الليلي والنهاري ولا يتراكب مع المحتوى عبر `scrollbar-gutter: stable`.

---

## تفاصيل المرحلة الثالثة (تمت): التحديث اللحظي الذكي Realtime دون إعادة تحميل الصفحة
1. **إنشاء موزع Realtime مركزي (`src/lib/realtime.ts`):** 
   - يدعم دمج أحداث Postgres Changes مباشرة في كاش TanStack Query عبر `queryClient.setQueryData`.
   - إضافة وإلغاء وتعديل السجلات في الكاش اللحظي دون الحاجة لطلب الصفحة أو إعادة جلب البيانات كاملة من السيرفر.
2. **تطوير التحديث اللحظي في نقاط البيع POS (`src/routes/_app.pos.tsx`):**
   - استبدال دالة `loadAll()` العنيفة (التي كانت تجلب 11 جدولاً دفعة واحدة عند كل حركة) بمعالجات دقيقة وموجهة:
     - عند تعديل أو إضافة منتج: يتم تحديث مصفوفة المنتجات فقط في الذاكرة.
     - عند إضافة أو تعديل عميل أو مخزن: يتم تحديث قائمة العملاء أو المخازن المستهدفة دون مساس ببقية الشاشة.
     - عند تغير أرصدة المخزون: يتم تحديث خريطة المخزون (`stockMap`) للمنتج والمخزن الحالي مباشرة.

---

## ضمان سلامة البيانات وقاعدة البيانات
- لا توجد أي استعلامات تخريبية (`DROP`, `TRUNCATE`, `DELETE` جماعي).
- جميع التغييرات تعمل بالتوافق الكامل مع بنية جداول وقواعد RLS في Supabase.
